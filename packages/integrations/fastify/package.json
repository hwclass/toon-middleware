{
  "name": "@toon-middleware/fastify",
  "version": "1.0.0",
  "description": "Fastify plugin for TOON middleware",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage",
    "test:watch": "node --test --watch",
    "lint": "eslint src/ tests/"
  },
  "keywords": [
    "fastify",
    "fastify-plugin",
    "middleware",
    "toon",
    "llm",
    "token-optimization"
  ],
  "dependencies": {
    "@toon-middleware/core": "workspace:*",
    "@toon-middleware/utils": "workspace:*",
    "@toon-middleware/cache": "workspace:*",
    "@toon-middleware/logger": "workspace:*",
    "fastify-plugin": "^5.0.0"
  },
  "peerDependencies": {
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "fastify": "^5.6.0"
  }
}
